{
  "name": "inspection-system-frontend",
  "private": true,
  "scripts": {
    "web:dev": "pnpm --filter @inspection/web dev",
    "mobile:start": "pnpm --filter @inspection/mobile start",
    "shared:build": "pnpm --filter @inspection/shared build",
    "typecheck": "pnpm -r typecheck"
  },
  "dependencies": {
    "recharts": "^3.7.0"
  }
}
