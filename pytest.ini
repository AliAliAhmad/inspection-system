[pytest]
# Exclude remote smoke tests by default (they need BASE_URL env var).
# Run them explicitly: pytest -m remote
addopts = -m "not remote"
markers =
    remote: tests that require a live deployment (BASE_URL env var)
